public class SomeTrash {


/*

    private double getDistance(double[] tmp) {

        for (int i = 0; i < dataForPerceptron.length; i++){
            dataForPerceptron[i] = dataForPerceptron[i]/getDistance(dataForPerceptron);
        }
        double distance = 0;
        for (double d : tmp){
            distance += Math.pow(d, 2);
        }
        distance = Math.sqrt(distance);
        return distance;
    }
*/


}
